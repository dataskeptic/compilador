
function soma(a, b) {
  return a + b;
}

function multiplica(x, y) {
  let resultado = x * y;
  return resultado;
}

function saudacao() {
  print("Olá!");
}

let num1 = 10;
let num2 = 5;

saudacao();

let total = soma(num1, num2);

let produto = multiplica(2, 3);

if (total > 10) {
  print("Total é maior que 10");
}

function verificaPositivo(numero) {
  if (numero > 0) {
    return true;
  } else {
    return false;
  }
}

let ehPositivo = verificaPositivo(total);
